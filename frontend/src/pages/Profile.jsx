import React from 'react'

export default function Profile() {
  return (
    <div>
      <h1>Profile</h1>
      <p>Move markup and logic from `profile.html` / `profile.js` here.</p>
    </div>
  )
}
